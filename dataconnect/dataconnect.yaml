specVersion: "v1"
serviceId: "tus"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tus-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
